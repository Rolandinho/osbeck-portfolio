{
  "name": "osbeck-portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve --watch",
    "extract-exif": "node scripts/extract-exif.js",
    "prebuild": "npm run extract-exif"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-img": "^3.1.0",
    "exifr": "^7.1.3"
  }
}
